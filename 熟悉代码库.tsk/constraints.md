# Constraints

- 只通过 `change_mind` 更新封装差遣牒（`*.tsk/`）；禁止对 `*.tsk/` 目录使用通用文件工具。
- 以 `dominds/docs/**` 与 `dominds/main/**`/`dominds/webapp/**` 的实现为准，输出的“快速定位索引”必须指向具体文件路径与关键符号（避免口头化总结）。
- 记录要可复用：命令/路径优先写成与 repo root 相对路径；区分 repo root rtws 与 `ux-rtws/`。