# Constraints

- 变更原则：一次只改一个 priming 变量；优先根因修复与可观测性提升，避免无关重构。
- 记录要求：每次实验必须能复现（输入、环境、步骤、期望/实际、日志/截图摘要）。
- 兼容策略：允许破坏性改动，但必须提供清晰回滚路径与回归清单（Dominds alpha/dogfooding-first）。
- i18n：若引入/修改用户可见文案，保持 `zh` 为语义源并同步 `en`。
- 环境隔离：如需启动 WebUI 开发实例，优先使用 `./dev-server.sh`（使用 `ux-rtws/`，避免污染根工作区）。