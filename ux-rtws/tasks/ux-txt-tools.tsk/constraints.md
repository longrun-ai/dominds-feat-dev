# Constraints

- Do not access or modify anything under `*.tsk/` via general file tools.
- Keep edits limited to `scratch/**` test files inside the rtws.
- Prefer fixed hunk ids (e.g. `!e2e_txt1`) so the test doesn’t need to parse tool output.
