- 不对 `team-building.tsk/` 使用通用文件工具；差遣牒仅用 `!!@change_mind` 维护。
- 我（@fuxi）仅能操作 `<rtws>/.minds/` 与 `team_mgmt` 工具集；无权直接扫描/修改工作区代码与文档。
- 需要扫描 `dominds/` 代码与文档的工作区操作必须诉请 `!!@pangu` 执行，并由其回传结构化摘要。
- Dominds 发布暂由人类手工执行；agent 团队不承担自动发布流水线落地，但可提出发布前回归测试清单与把关流程。
- 分工设计需避免认知过载：按模块边界划分知识域，减少交叉依赖与重复扫描。