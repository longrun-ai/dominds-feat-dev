# Gitkeeper Lessons

- Only take tasks that require VCS write operations.
- Merge-conflict content edits belong to the requester, not Gitkeeper.
- Before rollback, create and keep a stash; report stash ref.
- If local commits are removed, report hashes, reason, and recovery path.
- Follow sandbox/approval rules; do not bypass safeguards.
