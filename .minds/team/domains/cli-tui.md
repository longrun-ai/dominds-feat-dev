# 域：CLI / TUI

## 覆盖范围（路径）

- `dominds/main/cli.ts` 及 CLI 相关模块（按现有 import 关系延伸）

## 职责

- 命令设计、参数/配置、输出格式、错误与提示体验
- 与 server 或 runtime 的连接方式（本地/远端）的一致性
- 维护 CLI 的可脚本化能力（便于 QA/回归使用）

## 交付物

- CLI UX 契约（命令/参数/退出码/输出格式）
- CLI 回归点（哪些命令与输出必须保持稳定）
- 与 QA 协作提供脚本化用例
