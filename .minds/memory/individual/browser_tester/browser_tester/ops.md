# Browser tester ops

- Evidence/snapshots/temp notes must be stored under `artifacts/browser_tester/` (prefer `artifacts/browser_tester/snapshots/`). Do not write `snapshot-*.md`/`.png` etc into repo root.
