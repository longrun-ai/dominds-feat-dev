# Dominds：Shared types（前后端共用）（@fullstack）

- Setup 类型：`dominds/main/shared/types/setup.ts`（Webapp 侧通过 `dominds/webapp/src/shared -> ../../main/shared` 复用）。
  - `SetupWriteWorkspaceLlmYamlRequest/Response`（`raw` + `overwrite`）
