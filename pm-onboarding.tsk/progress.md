pm-onboarding（Dominds 现状理解）— Progress
- 2026-01-23：基于实际使用体验，确认 `!?@change_mind` 的正文必须用 `!?` 逐行提供；否则诉请块会在第一条普通行处结束，导致“需要提供差遣牒内容”。
- 体验痛点（P0）：tellask 逐行前缀规则 + `@change_mind` “必须有正文”叠加，导致高频脚枪：`ERR_MALFORMED_TELLASK` 与 “需要提供差遣牒内容”。
- 改进建议（面向源码落地）：
  - P0：为 `@change_mind` 增加“多行正文可不加 `!?` 前缀”的特例解析（兼容保留旧写法）。
  - P0：当 `@change_mind` body 为空时，报错提示明确指出“正文需要逐行 `!?`（当前实现）并给出示例”。
  - P0：同一条消息多次 `@change_mind` 时，解析/报错需要更友好（提示必须用普通行分隔不同诉请块）。
  - P1：更新 `dominds/docs/encapsulated-task-doc.md` 的示例与“常见错误”段，降低首次上手成本。
  - P2：WebUI 编辑 task doc 自动生成合法 tellask call，避免手打协议。
- 已有事实锚点（供后续回指）：
  - `dominds/docs/encapsulated-task-doc.md`：定义 `*.tsk/` 与 `!?@change_mind` 语义（one section, replace whole content, no round reset）。
  - `dominds/main/dialog.ts`：`diligenceAutoContinueCount`、`hasPendingQ4H()`/`canDrive()`、mental clarity 工具说明文本中含 `!?@change_mind` 约束提示。
  - `dominds/main/shared/i18n/driver-messages.ts`：包含 `ERR_MALFORMED_TELLASK` 相关错误文案与建议流程（distill->change_mind->clear_mind）。
  - `dominds/main/utils/task-doc.ts` / `dominds/main/utils/task-package.ts`：生成 task doc 提示文本与缺失分段提示里含 `!?@change_mind` 用法。